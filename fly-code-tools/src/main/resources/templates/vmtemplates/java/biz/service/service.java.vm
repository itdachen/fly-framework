package ${package}.service;

import ${package}.entity.${ClassName};
import ${package}.sdk.dto.${ClassName}DTO;
import ${package}.sdk.query.${ClassName}Query;
import ${package}.sdk.vo.${ClassName}VO;
import com.github.itdachen.framework.webmvc.service.IBizService;

/**
 * ${functionName} 业务接口
 *
 * @author ${author}
 * @date ${datetime}
 */
public interface I${ClassName}Service extends IBizService< ${ClassName}DTO, ${ClassName}VO, ${ClassName}Query, ${primaryKey} > {

}
